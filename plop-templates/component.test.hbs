import React from 'react';
import { render, screen } from '@testing-library/react';
import { userEvent } from '@testing-library/user-event';

import {{pascalCase componentName}} from './{{pascalCase componentName}}';

describe('{{pascalCase componentName}} Tests', () => {
 
  test('{{pascalCase componentName}} must render correctly', () => {
        //TODO: Please render your component and check the rendering
        // is proper by adding assertions to verify that expected DOM
        // elements are rendered correctly. 
       render(< {{pascalCase componentName}} />)
  });
});
